STEP 1
the library --> then load the file/data set --> check if load correctly

STEP 2
make the chart and name data correctly

def compute_cost(m, b, x, y):
prediction = m \* x + b
errors = y - prediction
mse = np.mean(errors \*\* 2)
return mse

study this part of the code
